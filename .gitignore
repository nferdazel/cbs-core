# Operating System
.DS_Store
Thumbs.db

# Dependencies
node_modules/
.pnpm-store/

# Build artifacts
dist/
build/
.next/
out/
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.out

# Turborepo
.turbo/

# Environment variables & Secrets (CRITICAL for public repo)
.env
.env.*
!.env.example
*.env
*.pem
*.key
id_rsa
id_ed25519

# Go
apps/core-api/core-api
# Biner hasil build lokal (bukan artefak sumber). `go build ./cmd/x` tanpa -o
# menaruh biner di direktori paket; dua perintah cmd sempat ter-commit karena pola
# sebelumnya hanya menutup `server`.
apps/api/server
apps/api/backfill-name-tokens
apps/api/reindex-customer-indexes

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Database & IDE
*.sqlite
*.db
.idea/
.vscode/
*.swp

# Redesign Tasklist
REDESIGN_TASKLIST.md

# Rework planning (local only)
DESIGN.md
BACKLOG.md


# Go workspace lock (lokal, bukan artifact build)
go.work.sum

# TypeScript incremental build info (artifact lokal)
*.tsbuildinfo

# Host SSH VPS (lokal, tidak untuk repo publik)
scripts/.ssh-host

# Backlog kerja (lokal, tidak untuk publikasi)
/docs/BACKLOG.md
